# Core cheminformatics (Windows-native, ships today)
rdkit>=2024.3.1
numpy>=1.26
scipy>=1.12
pandas>=2.2
pyarrow>=15.0

# Optional quantum/chem engines (install manually on Linux/WSL/conda):
#   pip install pyscf        # needs BLAS on Windows — use conda-forge
#   pip install pyqpanda     # Python 3.11 only as of 2026
# The worker falls back to RDKit-only automatically when these are absent.

# Data
requests>=2.31
tqdm>=4.66

# Storage + publish
huggingface_hub>=0.24
python-dotenv>=1.0

# Scheduling
apscheduler>=3.10

# Marketplace publishers (optional but cheap to install)
kaggle>=1.6

# HTTP API (optional — only needed if you run api.py)
fastapi>=0.110
uvicorn>=0.27
typer>=0.12
python-multipart>=0.0.20
email-validator>=2.0
